services:
  jenkins:
    image: jenkins/jenkins:lts
    container_name: jenkins
    user: root
    privileged: true
    ports:
      - "8080:8080"
      - "50000:50000"
      - "2443:2443"  # OpenBMC HTTPS port
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
      - ./romulus:/var/jenkins_home/workspace/romulus
      - ./web_ui_tests:/var/jenkins_home/workspace/web_ui_tests
      - ./redfish_api_tests:/var/jenkins_home/workspace/redfish_api_tests
      - ./load_tests:/var/jenkins_home/workspace/load_tests
      - ./chromedriver:/var/jenkins_home/workspace/chromedriver
      - ./scripts:/var/jenkins_home/workspace/scripts
      - ./jenkins_ssh:/var/jenkins_home/.ssh
    environment:
      - JAVA_OPTS=-Djenkins.install.runSetupWizard=false
      - GIT_SSH_COMMAND=ssh -o StrictHostKeyChecking=no
    command: >
      bash -c "
        apt-get update && 
        apt-get install -y python3 python3-pip python3-venv python3-full curl wget git openssh-client qemu-system-arm qemu-utils kvm libvirt-daemon-system libvirt-clients bridge-utils &&
        apt-get install -y chromium-browser chromium-chromedriver xvfb &&
        apt-get install -y build-essential libssl-dev libffi-dev python3-dev &&
        
        # Установка Python зависимостей
        pip3 install --upgrade pip &&
        pip3 install selenium pytest requests urllib3 locust &&
        pip3 install webdriver-manager pytest-html pytest-xvfb &&
        
        # Настройка Chrome для headless режима
        echo '#!/bin/bash' > /usr/local/bin/chrome-wrapper &&
        echo 'exec /usr/bin/chromium-browser --no-sandbox --disable-dev-shm-usage --disable-gpu --headless --remote-debugging-port=9222 \"$$@\"' >> /usr/local/bin/chrome-wrapper &&
        chmod +x /usr/local/bin/chrome-wrapper &&
        
        # Настройка SSH
        mkdir -p /var/jenkins_home/.ssh &&
        chmod 700 /var/jenkins_home/.ssh &&
        
        # Настройка прав для QEMU
        chmod +x /usr/bin/qemu-system-arm &&
        
        # Запуск Jenkins
        /usr/local/bin/jenkins.sh
      "

  dind:
    image: docker:dind
    container_name: dind
    privileged: true
    environment:
      DOCKER_TLS_CERTDIR: ""

volumes:
  jenkins_home:
